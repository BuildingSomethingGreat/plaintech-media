# plaintech-media

Public image host for the @plaintech.business Threads page.

Threads fetches post images server-side, so they must be reachable without
authentication. This repository exists only to serve those files — it holds no
code, configuration or strategy. Images are generated by `agent/imagegen.py`
in the private agent repository.
